# Venu Skills Registry — 2026-09-01 (superseded)

**Superseded by Phase 1.** This file listed only the 47 technical/AI
Skills and had drifted from the root registry, which also covers the 6
Business Mastery Skills and the 5 cross-cutting extensions (58 total). Per
the Phase 1 decision, the repository must not maintain multiple
independent naming registries.

The single authoritative, machine-readable registry is now
`/SKILL-REGISTRY.json` at the repository root, generated by
`venu-runtime/scripts/build_registry.py` from the contents of `skills/` —
see `docs/ARCHITECTURE.md` for the schema.

This file is kept (not deleted) for history; do not edit it further or
treat it as a source of truth.
